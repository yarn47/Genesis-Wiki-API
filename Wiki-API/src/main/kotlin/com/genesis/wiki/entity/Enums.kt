package com.genesis.wiki.entity

enum class Grade { rare, hero, legend, outer }
enum class Faction { geysir, pendragon, independent, astania, zephyrfalcon, dagal }
enum class Element { light, dark, fire, crystal, nature }
enum class DefenseType { light, medium, heavy }
enum class SkillType { active, passive }
enum class UnlockType { awaken, manifest }
enum class SourceType { skill, class_skill, passive, ultimate, artifact, item }
enum class EffectType { normal, exclusive }
enum class EquipmentType { helmet, armor, gloves, boots, accessory }
enum class EquipmentGrade { rare, hero, legend }